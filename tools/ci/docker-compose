#!/usr/bin/env bash
# Run Docker Compose as the host runner user to avoid root-owned vendor files.
set -euo pipefail

export DOCKER_UID="${DOCKER_UID:-$(id -u)}"
export DOCKER_GID="${DOCKER_GID:-$(id -g)}"

exec docker compose "$@"
