# Groups

Group wrappers are available through:

- `groups()`
- `groupsMembers()`
- `groupsPools()`
- `groupsDiscussTopics()`
- `groupsDiscussReplies()`

Wrappers accept Flickr parameter arrays and return `ApiResponseData`.
